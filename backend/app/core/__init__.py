"""
Purpose: Core infrastructure — config, exceptions, security, logging, API response models.
Architecture Fit:
  - Loaded at application boot before any domain modules.
  - Provides shared utilities used across all layers.
"""
